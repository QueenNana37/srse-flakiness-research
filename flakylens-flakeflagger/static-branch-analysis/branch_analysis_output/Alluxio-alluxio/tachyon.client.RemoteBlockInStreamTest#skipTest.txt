Test: tachyon.client.RemoteBlockInStreamTest#skipTest
STATUS: parsed successfully
Summary: 2 branch point(s), 24 method call(s), 10 unique method(s) called

--- Line 3: for ---
for (int k = MIN_LEN + DELTA; k <= MAX_LEN; k += DELTA) {
      WriteType op = WriteType.THROUGH;
      int fileId = TestUtils.createByteFile(mTfs, "/root/testFile_" + k + "_" + op, op, k);

      TachyonFile file = mTfs.getFile(fileId);
      InStream is = file.getInStream(ReadType.CACHE);
      Assert.assertTrue(is instanceof RemoteBlockInStream);
      Assert.assertEquals(k / 2, is.skip(k / 2));
      Assert.assertEquals(k / 2, is.read());
      is.close();
      Assert.assertFalse(file.isInMemory());

      if (k >= 3) {
        is = file.getInStream(ReadType.CACHE);
        Assert.assertTrue(is instanceof RemoteBlockInStream);
        int t = k / 3;
        Assert.assertEquals(t, is.skip(t));
        Assert.assertEquals(t, is.read());
        Assert.assertEquals(t, is.skip(t));
        Assert.assertEquals(2 * t + 1, is.read());
        is.close();
        Assert.assertFalse(file.isInMemory());
      }
    }
  Calls inside this branch:
    Assert.assertEquals: called 2x (line(s): 10, 11)
    TestUtils.createByteFile: called 1x (line(s): 5)
    mTfs.getFile: called 1x (line(s): 7)
    file.getInStream: called 1x (line(s): 8)
    Assert.assertTrue: called 1x (line(s): 9)
    is.skip: called 1x (line(s): 10)
    is.read: called 1x (line(s): 11)
    is.close: called 1x (line(s): 12)
    Assert.assertFalse: called 1x (line(s): 13)
    file.isInMemory: called 1x (line(s): 13)

--- Line 15: if ---
if (k >= 3) {
        is = file.getInStream(ReadType.CACHE);
        Assert.assertTrue(is instanceof RemoteBlockInStream);
        int t = k / 3;
        Assert.assertEquals(t, is.skip(t));
        Assert.assertEquals(t, is.read());
        Assert.assertEquals(t, is.skip(t));
        Assert.assertEquals(2 * t + 1, is.read());
        is.close();
        Assert.assertFalse(file.isInMemory());
      }
  Calls inside this branch:
    Assert.assertEquals: called 4x (line(s): 19, 20, 21, 22)
    is.skip: called 2x (line(s): 19, 21)
    is.read: called 2x (line(s): 20, 22)
    file.getInStream: called 1x (line(s): 16)
    Assert.assertTrue: called 1x (line(s): 17)
    is.close: called 1x (line(s): 23)
    Assert.assertFalse: called 1x (line(s): 24)
    file.isInMemory: called 1x (line(s): 24)

