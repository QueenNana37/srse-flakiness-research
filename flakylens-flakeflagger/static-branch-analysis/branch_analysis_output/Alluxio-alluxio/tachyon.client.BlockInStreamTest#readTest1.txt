Test: tachyon.client.BlockInStreamTest#readTest1
STATUS: parsed successfully
Summary: 8 branch point(s), 21 method call(s), 8 unique method(s) called

--- Line 3: for ---
for (int k = MIN_LEN; k <= MAX_LEN; k += DELTA) {
      for (WriteType op : WriteType.values()) {
        int fileId = TestUtils.createByteFile(mTfs, "/root/testFile_" + k + "_" + op, op, k);

        TachyonFile file = mTfs.getFile(fileId);
        InStream is = (k < MEAN ?
            file.getInStream(ReadType.CACHE) : file.getInStream(ReadType.NO_CACHE));
        if (k == 0) {
          Assert.assertTrue(is instanceof EmptyBlockInStream);
        } else {
          Assert.assertTrue(is instanceof BlockInStream);
        }
        byte[] ret = new byte[k];
        int value = is.read();
        int cnt = 0;
        while (value != -1) {
          ret[cnt ++] = (byte) value;
          value = is.read();
        }
        Assert.assertTrue(TestUtils.equalIncreasingByteArray(k, ret));
        is.close();

        is = (k < MEAN ? file.getInStream(ReadType.CACHE) : file.getInStream(ReadType.NO_CACHE));
        if (k == 0) {
          Assert.assertTrue(is instanceof EmptyBlockInStream);
        } else {
          Assert.assertTrue(is instanceof BlockInStream);
        }
        ret = new byte[k];
        value = is.read();
        cnt = 0;
        while (value != -1) {
          ret[cnt ++] = (byte) value;
          value = is.read();
        }
        Assert.assertTrue(TestUtils.equalIncreasingByteArray(k, ret));
        is.close();
      }
    }

--- Line 4: for ---
for (WriteType op : WriteType.values()) {
        int fileId = TestUtils.createByteFile(mTfs, "/root/testFile_" + k + "_" + op, op, k);

        TachyonFile file = mTfs.getFile(fileId);
        InStream is = (k < MEAN ?
            file.getInStream(ReadType.CACHE) : file.getInStream(ReadType.NO_CACHE));
        if (k == 0) {
          Assert.assertTrue(is instanceof EmptyBlockInStream);
        } else {
          Assert.assertTrue(is instanceof BlockInStream);
        }
        byte[] ret = new byte[k];
        int value = is.read();
        int cnt = 0;
        while (value != -1) {
          ret[cnt ++] = (byte) value;
          value = is.read();
        }
        Assert.assertTrue(TestUtils.equalIncreasingByteArray(k, ret));
        is.close();

        is = (k < MEAN ? file.getInStream(ReadType.CACHE) : file.getInStream(ReadType.NO_CACHE));
        if (k == 0) {
          Assert.assertTrue(is instanceof EmptyBlockInStream);
        } else {
          Assert.assertTrue(is instanceof BlockInStream);
        }
        ret = new byte[k];
        value = is.read();
        cnt = 0;
        while (value != -1) {
          ret[cnt ++] = (byte) value;
          value = is.read();
        }
        Assert.assertTrue(TestUtils.equalIncreasingByteArray(k, ret));
        is.close();
      }
  Calls inside this branch:
    Assert.assertTrue: called 4x (line(s): 13, 22, 29, 38)
    is.read: called 2x (line(s): 16, 32)
    TestUtils.equalIncreasingByteArray: called 2x (line(s): 22, 38)
    is.close: called 2x (line(s): 23, 39)
    WriteType.values: called 1x (line(s): 4)
    TestUtils.createByteFile: called 1x (line(s): 5)
    mTfs.getFile: called 1x (line(s): 7)

--- Line 8: ternary ---
k < MEAN ?
            file.getInStream(ReadType.CACHE) : file.getInStream(ReadType.NO_CACHE)
  Calls inside this branch:
    file.getInStream: called 2x (line(s): 9, 9)

--- Line 10: if ---
if (k == 0) {
          Assert.assertTrue(is instanceof EmptyBlockInStream);
        }
  Calls inside this branch:
    Assert.assertTrue: called 1x (line(s): 11)

--- Line 18: while ---
while (value != -1) {
          ret[cnt ++] = (byte) value;
          value = is.read();
        }
  Calls inside this branch:
    is.read: called 1x (line(s): 20)

--- Line 25: ternary ---
k < MEAN ? file.getInStream(ReadType.CACHE) : file.getInStream(ReadType.NO_CACHE)
  Calls inside this branch:
    file.getInStream: called 2x (line(s): 25, 25)

--- Line 26: if ---
if (k == 0) {
          Assert.assertTrue(is instanceof EmptyBlockInStream);
        }
  Calls inside this branch:
    Assert.assertTrue: called 1x (line(s): 27)

--- Line 34: while ---
while (value != -1) {
          ret[cnt ++] = (byte) value;
          value = is.read();
        }
  Calls inside this branch:
    is.read: called 1x (line(s): 36)

